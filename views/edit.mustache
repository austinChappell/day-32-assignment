<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Mugs</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  </head>
  <body>

    <nav>
      <a href="/"><i class="fa fa-coffee" aria-hidden="true"></i></a>
      <h1><a href="/">Mugs Central</a></h1>
      <a class="add-link" href="/add"><i class="fa fa-plus" aria-hidden="true"></i>
</a>
    </nav>
    </nav>

    <div class="container">

      <h1>Edit Mug</h1>
      <form action="/edit/{{ mug._id }}?_method=PUT" method="post">
        <div class="form-group">
          <label>Mug Name:</label><br />
          <input type="text" name="name" value="{{ mug.name }}" />
        </div>
        <div class="form-group">
          <label>Color:</label><br />
          <input type="text" name="color" value="{{ mug.appearance.color }}" />
        </div>
        <div class="form-group">
          <label>Type:</label><br />
          <select name="style">
            {{{ style }}}
          </select>
        </div>
        <div class="form-group">
          <label>Material:</label><br />
          <select name="material">
            {{{ material }}}
          </select>
        </div>
        <div class="form-group">
          <label>Is this mug dishwasher safe?</label><br />
          {{{ dishwasherSafe }}}
        </div>
        <div class="form-group">
          <label>Is this mug insulated?</label><br />
          {{{ insulated }}}
        </div>
        <div class="form-group">
          <label>How many ounces?</label><br />
          <select name="sizeInOunces">
            {{{ size }}}
          </select>
        </div>
        <div class="form-group">
          <label>Unique Characteristics (separate characteristics with semicolons)</label><br />
          <input type="text" name="uniqueCharacteristics" value="{{ characteristics }}" />
        </div>
        <input id="edit-button" type="submit" value="Update Mug" />
      </form>


    </div>
  </body>
</html>
